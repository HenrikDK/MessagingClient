<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>netstandard2.0</TargetFramework>
        <IsPackable>true</IsPackable>
        <NuspecFile>.nuspec</NuspecFile>
        <PackageVersion>1.0.1</PackageVersion>
        <Title>Messaging.Client</Title>
        <Authors>HenrikDK</Authors>
        <Description>Generic Messaging client implements a delegating consumer workflow and encapsulates the implementation details of communicating with Azure Event Hub</Description>
        <PackageProjectUrl>tbd</PackageProjectUrl>
        <PackageTags>azure event hub messaging delegating consumer</PackageTags>
        <LangVersion>default</LangVersion>
    </PropertyGroup>

    <ItemGroup>
      <PackageReference Include="Azure.Messaging.EventHubs.Processor" Version="5.6.2" />
      <PackageReference Include="Microsoft.CSharp" Version="4.7.0" />
    </ItemGroup>

</Project>
